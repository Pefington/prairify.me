<!DOCTYPE html>
<html>
  <head>
    <title>prairify.me</title>
    <link
      rel="icon"
      type="image/png"
      href="<%= asset_path 'logo.png' %>"
    />
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/e05ffa89c7.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "https://fonts.googleapis.com/css2?family=Fjalla+One&family=Quicksand:wght@500;600&display=swap", "data-turbo-track": "reload" %>
    <%= javascript_tag 'application' %>
    <%= javascript_importmap_tags %>
  </head>
  <body
    class="
      flex
      flex-col
      items-center
      h-full
      min-h-screen
    "
  >
    <%= render 'layouts/navbar' %>
    <%= render 'shared/flashes' %>
    <main class="flex flex-col flex-1 items-center w-full pt-12 pb-16">
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
