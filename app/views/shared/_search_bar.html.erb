<%= form_with url: search_index_path, method: :get,
  class: "flex items-center ring-2 ring-middle rounded-md w-full bg-inverse lg:mx-2 xl:mx-8" do |form| %>
  <i class="fa-solid fa-magnifying-glass-location text-neutral-light/50 px-2">
  </i>
  <input type="search" name="search" placeholder=
  <%= t'holdersearch' %>
  class="bg-inverse p-2 text-neutral-light flex-1 rounded-r-md focus:ring-0
  pl-0 focus:text-middle">
<% end %>
